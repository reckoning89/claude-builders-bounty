[project]
name = "claude-builders-bounty"
version = "0.1.0"
description = "Community bounty board utilities for Claude Code builders"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tools"]
